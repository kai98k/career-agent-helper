# 複製成 .env 後填入自己的值；.env 不進版控
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=global
# 新名稱；舊名 GOOGLE_GENAI_USE_VERTEXAI 仍可用，衝突時以本項為準
GOOGLE_GENAI_USE_ENTERPRISE=True

# 模型
GEMINI_MODEL=gemini-2.5-flash

# 應用
APP_ENV=local
MAX_UPLOAD_MB=5
MAX_PDF_PAGES=10
